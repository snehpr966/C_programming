#include <stdio.h>

int main()
{

printf("Enter the dimensions of rectangle");
int a,b,c,d;
scan("%d %d",$a,$b);
b = 2*(l+b);
α =l*b;
printf("The perimeter = %d \n area=%d",c,d);
return 0;
}